import { test, expect } from 'vitest';
import { circularArrayRotation } from './circular-array-rotation';
test('Test case 01 example', () => {
  // expect(circularArrayRotation([1, 2, 3],2, [0, 1])).toEqual([5, 3]);
});
